#include "IntegralFeaturesContainer.h"

namespace vca
{
  namespace icf
  {
    //IntegralFeaturesContainer::IntegralFeaturesContainer( IntegralFeatures& icf ) : icf( &icf )
    //{
    //  this->icf->registerObserver( this );
    //}

    //IntegralFeaturesContainer::~IntegralFeaturesContainer()
    //{
    //  /* Todo: automatisches Löschen aller wavelets */
    //  icf->deregisterObserver( this );
    //}

    //void IntegralFeaturesContainer::updateFeatureChannelPointer( map< Vca::Icf::ChannelType, cv::Mat >& integralMap )
    //{
    //  boxes.updateFeatureChannelPointer( integralMap);
    //  //for( auto i = 0; i < boxes.numBoxtypes(); i++ )
    //  //{
    //  //  for( auto j = 0; j < boxes.size(i); j++)
    //  //  {
    //  //    boxes.setBox(integralMap, i, j);
    //  //  }
    //  //}
    //}
  }
}
